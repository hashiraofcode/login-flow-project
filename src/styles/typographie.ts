import styled from 'styled-components'

export const MainTitle = styled.h1`
  font: 500 clamp(1.9rem, 3vw, 2.9rem) inherit;
  color: #000;
  padding: 2rem;
`

export const subTitles = styled.p`
  font: 300 clamp(1rem, 2vw, 1.5rem) inherit;
  color: #999;
  padding: 0.9rem;
`
export const LinkRegistration = styled.a`
  font: 300 clamp(1rem, 2vw, 1.5rem) inherit;
  color: #000;
  padding: 0.9rem;
`
